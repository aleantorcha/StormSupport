import React, {Component} from 'react';
import { Platform, StyleSheet, Text, View, Button } from 'react-native';

export default class PinInput extends Component {
  render(){
    return (
      <View >
        <Text>This is Pin Input screen</Text> 
      </View>
    );
  }
}
